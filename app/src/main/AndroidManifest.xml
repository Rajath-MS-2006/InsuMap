<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <queries>
        <package android:name="com.google.android.gms" />
        <intent>
            <action android:name="com.google.mlkit.vision.documentscanner.ACTION_SCAN_DOCUMENT" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.InsuranceClaims"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.SignupActivity" />
        <activity android:name=".activities.SelectRoleActivity" />
        <activity 
            android:name=".activities.MainActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity 
            android:name=".activities.UploadPolicyActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity 
            android:name=".activities.AdjudicationActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity android:name=".activities.ClaimDetailActivity" />
        <activity android:name=".activities.AddClaimActivity" />
        <activity 
            android:name=".activities.ClaimHistoryActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity 
            android:name=".activities.ProfileActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity 
            android:name=".activities.ExpenseTrackerActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity
            android:name=".activities.OnboardingActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity
            android:name=".activities.FraudDashboardActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity
            android:name=".activities.InsuranceCardActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />
        <activity
            android:name=".activities.AppealClaimActivity"
            android:theme="@style/Theme.InsuranceClaims.NoActionBar" />


        <service
            android:name=".services.InsuMapMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
